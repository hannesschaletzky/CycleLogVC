//
//  z_BackupCode.swift
//  CycleLogVC
//
//  Created by Hannes Schaletzky on 14.05.20.
//  Copyright © 2020 Hannes Schaletzky. All rights reserved.
//

import Foundation

/*
 
 func setIconImg(img:String) {
     cell.imageType.image = UIImage(systemName: img)
 }
 
 func setIconCol(col:UIColor) {
     cell.imageType.tintColor = col
 }
 
 
 if (log.type == Log.Type_JourneyStart || log.type == Log.Type_JourneyEnd) {
     setIconImg(img: "globe")
     setIconCol(col: UIColor.systemGray)
 }
 else if (log.type == Log.Type_DayStart) {
     setIconImg(img: "sun.max.fill")
     setIconCol(col: UIColor.systemYellow)
 }
 else if (log.type == Log.Type_DayEnd) {
     setIconImg(img: "moon.zzz.fill")
     setIconCol(col: UIColor.black)
 }
 else if (log.type == Log.Type_Checkpoint) {
     setIconImg(img: "mappin.and.ellipse")
     setIconCol(col: UIColor.lightGray)
 }
 else if (log.type == Log.Type_Drink) {
     setIconImg(img: "d.square")
     setIconCol(col: UIColor.lightGray)
 }
 else if (log.type == Log.Type_Food) {
     setIconImg(img: "f.square")
     setIconCol(col: UIColor.lightGray)
 }
 else if (log.type == Log.Type_Stay) {
     setIconImg(img: "house.fill")
     setIconCol(col: UIColor.lightGray)
 }
 else if (log.type == Log.Type_Friends) {
     setIconImg(img: "person.2.fill")
     setIconCol(col: UIColor.lightGray)
 }
 else if (log.type == Log.Type_Photo) {
     setIconImg(img: "photo")
     setIconCol(col: UIColor.lightGray)
 }
 
 
 
 
 
 
 
 
 @IBAction func loadImagesValueChanged() {
     logTableVC.tableView.setEditing(false, animated: true)
     logTableVC.loadImage = loadImagesSwitch.isOn
 }

 //image
 if (log.image1 != "" && loadImage) {
     let tp_retrievePic = retrieveImage(forKey: log.image1!)
     if (!tp_retrievePic.0) {
         showAlert(msg: "Error load image at \(indexPath.row)")
     }
     else {
         let thumb1 = tp_retrievePic.2!.resized(withPercentage: 0.1)
         cell.logImage.image = thumb1
     }
 }
 else {
     cell.logImage.image = nil
 }
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 
 */
